<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
    <select id="selectUserById" resultType="com.tcp.toeflserver.user.CustomUser">
        SELECT *
        FROM user
        WHERE id=#{id}
    </select>

    <select id="selectUserByEmail" resultType="com.tcp.toeflserver.user.CustomUser">
        SELECT *
        FROM user
        WHERE email=#{email}
    </select>
</mapper>